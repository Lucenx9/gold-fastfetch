# Ralph Progress Log
Started: mar 17 feb 2026, 01:29:39, CET
---

## Codebase Patterns
- scripts/lib.sh is the shared library — all scripts should `source` it for logging and checks
- Use `printf` instead of `echo -e` for colored output
- All shell scripts need `#!/usr/bin/env bash` and `set -euo pipefail`
- Run `shellcheck` on all `.sh` files before committing
- Git repo local config: user=Lucenx9, email=Lucenx9@users.noreply.github.com
- Use `${var,,}` for lowercase and `${var^}` for capitalize-first instead of piping to `sed`
- Config templates use `{{PLACEHOLDER}}` syntax (double curly braces) — replaced by `sed` at install time
- Config templates are valid JSON — can be validated with `python3 -c "import json; json.load(open('file'))"`

---

## 2026-02-17 - US-001
- What was implemented: Verified existing scripts/lib.sh meets all acceptance criteria
- Files changed: scripts/lib.sh (already existed, staged and committed)
- Functions present: log_info(), log_warn(), log_error(), die(), check_not_root(), check_arch(), check_command(), require_interactive()
- **Learnings for future iterations:**
  - lib.sh was already written and staged before Ralph started — just needed verification and commit
  - shellcheck passes clean with SC2034 disable for SCRIPT_DIR (unused in lib.sh itself but used by sourcing scripts)
  - Git identity needed manual setup (not configured globally on this machine)
---

## 2026-02-17 - US-002
- What was implemented: Extracted GPU detection script from install.sh heredoc (lines 143-182) into templates/scripts/gpu_detect.sh
- Files changed: templates/scripts/gpu_detect.sh (new file)
- **Learnings for future iterations:**
  - The GPU detect script is self-contained — no placeholders needed, uses runtime env vars and commands
  - shellcheck passes clean on the extracted script with no warnings
  - The templates/scripts/ directory is new — created for this and future script extractions (US-003, US-004)
---

## 2026-02-17 - US-003
- What was implemented: Extracted update checker script from install.sh heredoc (lines 185-258) into templates/scripts/updates.sh
- Files changed: templates/scripts/updates.sh (new file)
- **Learnings for future iterations:**
  - The updates script is self-contained — no placeholders needed, uses $XDG_CACHE_HOME at runtime
  - shellcheck passes clean on the extracted script with no warnings
  - Same extraction pattern as US-002: copy heredoc body, add shebang + set flags, verify with shellcheck
---

## 2026-02-17 - US-004
- What was implemented: Extracted disk detection script from install.sh heredoc (lines 261-381) into templates/scripts/disk_detect.sh
- Files changed: templates/scripts/disk_detect.sh (new file)
- **Learnings for future iterations:**
  - The disk detect script is self-contained — no placeholders needed
  - Had to fix two shellcheck warnings: SC2001 (use `${var^}` instead of `sed 's/^./\U&/'`) and SC2059 (don't use variables in printf format string)
  - Same extraction pattern as US-002/US-003: copy heredoc body, add shebang + set flags, fix shellcheck warnings, verify clean
  - `${fslabel,,}` lowercases, `${lower^}` capitalizes first letter — pure bash, no sed needed
---

## 2026-02-17 - US-005
- What was implemented: Created gold config template at templates/config-gold.jsonc with {{PLACEHOLDER}} markers
- Files changed: templates/config-gold.jsonc (new file)
- Template has 32 icon placeholders ({{I_USER}}, {{I_HOST}}, etc.) and 3 {{SCRIPTS_DIR}} references
- All modules preserved in original order: User/Host/Time, System, Hardware, Display/Audio, Media, Palette
- Logo padding (top:1, left:2, right:3) and box-drawing section separators included
- **Learnings for future iterations:**
  - Config templates are valid JSON (no comments used), so python3 json.load() works for validation
  - Use `{{PLACEHOLDER}}` style (double curly braces) — these get replaced by sed at install time
  - The gold template has 3 command modules that reference {{SCRIPTS_DIR}}: updates.sh, gpu_detect.sh, disk_detect.sh
  - The `$schema` key uses a literal `$` (not a placeholder) — be careful not to confuse it with shell vars
---

## 2026-02-17 - US-006
- What was implemented: Created minimal config template at templates/config-minimal.jsonc
- Files changed: templates/config-minimal.jsonc (new file)
- Modules included: OS, Kernel, Uptime, Shell, CPU, RAM, Disk (built-in fastfetch module), Color Palette
- Uses simple separator (" → "), single cyan accent color, no box-drawing borders
- 8 icon placeholders: {{I_OS}}, {{I_KER}}, {{I_UP}}, {{I_SH}}, {{I_CPU}}, {{I_RAM}}, {{I_DISK}}, {{I_PAL}}
- No {{SCRIPTS_DIR}} references — minimal variant doesn't use custom scripts
- **Learnings for future iterations:**
  - Minimal config uses fastfetch's built-in `disk` module type (not the custom disk_detect.sh script)
  - The `cd` command in zsh is intercepted by zoxide on this machine — use `git -C <path>` instead of `cd <path> && git`
  - Keep minimal template intentionally simple — resist adding extra modules
---

## 2026-02-17 - US-007
- What was implemented: Refactored install.sh to use templates and lib.sh instead of inline heredocs
- Files changed: install.sh (complete rewrite)
- Key changes:
  - Sources scripts/lib.sh for logging (log_info/log_warn/log_error), check_not_root(), check_arch(), check_command()
  - Added --variant gold|minimal flag (default: gold), validates against known values
  - Template processing: reads templates/config-<variant>.jsonc and replaces {{PLACEHOLDER}} via sed array
  - Gold variant: copies templates/scripts/*.sh to $CONFIG_DIR/scripts/ and chmod +x
  - Minimal variant: skips helper script installation entirely
  - All inline heredocs for config and helper scripts removed
  - Backup logic unchanged (same rotation, same paths, same 5-backup limit)
- **Learnings for future iterations:**
  - Use `sed` with pipe `|` as delimiter (not `/`) to avoid conflicts with path separators in {{SCRIPTS_DIR}}
  - Build sed args as a bash array (SED_ARGS) for clean multi-placeholder replacement
  - shellcheck SC1091 (info) about sourced files is expected when running shellcheck outside the repo root — use `-S warning` to filter
  - The `# shellcheck source=scripts/lib.sh` directive helps shellcheck follow sourced files when using `-x`
  - Icon variables contain multibyte Nerd Font glyphs — sed handles them fine with `|` delimiter
---

## 2026-02-17 - US-008
- What was implemented: Updated uninstall.sh to source scripts/lib.sh instead of defining its own colors and checks
- Files changed: uninstall.sh (refactored)
- Key changes:
  - Sources scripts/lib.sh via `source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/scripts/lib.sh"`
  - Removed duplicate color variables (GREEN, YELLOW, RED, NC)
  - Replaced inline root check with `check_not_root()` call
  - Replaced inline interactive check with `require_interactive()` call
  - Replaced `echo -e` calls with `log_info()`, `log_warn()`, `log_error()`, and `printf`
  - Used `die()` for the CONFIG_DIR sanity check error
  - Behavior unchanged: prompts user, removes config, restores latest backup
- **Learnings for future iterations:**
  - The `require_interactive()` check was moved before the prompt (cleaner flow — fail early)
  - Use `# shellcheck source=scripts/lib.sh` directive so shellcheck can follow the source with `-x`
  - Plain `printf` is appropriate for non-colored messages (no need for log_* wrappers)
---
